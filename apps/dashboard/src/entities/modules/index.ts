/**
 * Modules entity - Módulos dinámicos del sistema
 *
 * Los módulos se obtienen desde GET /api/modules (DB), no están hardcodeados.
 */

export { useModules } from "./model/use-modules";
export type { Module } from "./model/types";
